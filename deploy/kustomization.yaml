apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- namespace.yaml
- rbac.yaml
- pvc.yaml
- controller.yaml
# Opt in after validating cgroup-v2/BPF requirements; TCX/XDP interfaces are optional:
# - agent.yaml
# Optional Cilium policy integration:
# - rbac-cilium.yaml
