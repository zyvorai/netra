import { useState } from 'react';
import Nav,{Page} from './components/Nav';
import Overview from './pages/Overview';import Insights from './pages/Insights';import L7 from './pages/L7';import Policies from './pages/Policies';import Flows from './pages/Flows';import EBPF from './pages/EBPF';import Health from './pages/Health';import Audit from './pages/Audit';import Workloads from './pages/Workloads';import {setToken,token} from './api';
export default function App(){const[page,setPage]=useState<Page>('overview'),[tok,setTok]=useState(token());const body={overview:<Overview/>,pods:<Workloads kind="pod"/>,vms:<Workloads kind="vm"/>,health:<Health/>,l7:<L7/>,insights:<Insights/>,policies:<Policies/>,flows:<Flows/>,ebpf:<EBPF/>,audit:<Audit/>}[page];return <><Nav page={page} setPage={setPage}/><main><header className="hero"><div><p className="eyebrow">STANDALONE eBPF · OPTIONAL CILIUM</p><h1>See the network. Diagnose it. Contain it.</h1><p>Netra runs its own eBPF datapath for workload flows, TCP health, DNS timing, socket/process identity and leased emergency controls. Cilium and Hubble are optional enrichment.</p></div><label className="tokenbox">API token<input type="password" value={tok} placeholder="required unless dev mode" onChange={e=>{setTok(e.target.value);setToken(e.target.value)}}/></label></header>{body}</main></>}
